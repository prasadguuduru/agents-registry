{
  "name": "send-message-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc --project ./tsconfig.json && mkdir -p ../../build/sendMessageLambda && cp -r * ../../build/sendMessageLambda"
  },
  "dependencies": {
    "axios": "^1.8.3",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.320.0"
  }
}
